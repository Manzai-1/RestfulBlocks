import { catchErrorAsync } from "../utillities/catchErrorAsync.mjs";


export const listAllBlocks = catchErrorAsync( async (res, req) => {
    
});

export const addBlock = catchErrorAsync( async (res, req) => {
    
});

export const findBlock = catchErrorAsync( async (res, req) => {
    
});